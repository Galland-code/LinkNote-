import 'package:flutter/material.dart';

class AppColors {
  // Base colors
  static const Color primary = Color(0xFF9E2956);
  static const Color secondary = Color(0xFF4682B4);
  static const Color accent = Color(0xFFFFD700);

  // Background colors
  static const Color background = Color(0xFFF5F5DC); // Cream grid paper background
  static const Color cardBackground = Color(0xFFF0F8FF);
  static const Color buttonBackground = Color(0xFFC1446F);

  // Text colors
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF555555);
  static const Color buttonText = Color(0xFFFFFFFF);

  // Border colors
  static const Color buttonBorder = Color(0xFF000000);
  static const Color cardBorder = Color(0xFF000000);

  // Achievement colors
  static const Color trophyGold = Color(0xFFFFD700);
  static const Color trophyGreen = Color(0xFF90EE90);

  // Prevent instantiation
  AppColors._();
}